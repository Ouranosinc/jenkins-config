jenkins:
  globalNodeProperties:
  - envVars:
      env:
      - key: IS_PROD
        value: ${IS_PROD}  # Env var from env.local
      - key: ENABLE_SCHEDULED_TRIGGER
        value: ${ENABLE_SCHEDULED_TRIGGER}  # Env var from env.local


# vi: tabstop=8 expandtab shiftwidth=2 softtabstop=2
